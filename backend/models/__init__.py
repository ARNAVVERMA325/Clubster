# TODO: ORM-style row models mirroring database/schema.sql, once an ORM
# (e.g. SQLAlchemy) or typed table wrappers are chosen. Left empty for now —
# routers currently talk to Supabase directly via core.config.get_supabase_client.
